{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "outputDirectory": "dist/client",
  "framework": null,
  "regions": ["dub1"],
  "functions": {
    "api/server.mjs": {
      "includeFiles": "{dist/server/**,public/fonts/**}"
    }
  },
  "crons": [
    {
      "path": "/api/sheets-sync",
      "schedule": "0 3 * * *"
    }
  ],
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/api/server" }
  ]
}
