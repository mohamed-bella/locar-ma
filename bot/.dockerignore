node_modules
.env
*.log
npm-debug.log*
.DS_Store
# NOTE: auth_info is intentionally NOT ignored — it's baked as a one-time seed
# (relocated to /app/auth_seed in the Dockerfile) to avoid re-scanning the QR.
